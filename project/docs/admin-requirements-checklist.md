# Admin Requirements Checklist

Nguồn yêu cầu: `docs/admin-requirements-full.md`.

Quy ước trạng thái: [ ] chưa làm, [~] đang làm, [x] đã sửa code và đã kiểm tra. Mỗi lượt cập nhật chỉ đánh dấu các mã checklist thuộc nhóm được yêu cầu trong lượt đó.

## 1. Trang quản lý tỉnh/thành phố

File đã sửa/kiểm tra trong nhóm: `src/views/pages/admin/locations-provinces.pug`; `src/public/js/admin/locations.js`; `src/public/css/admin/locations.css`; `src/controllers/viewController.js`; `src/controllers/locationController.js`; `src/routes/locationRoutes.js`; `prisma/schema.prisma`; `src/config/init.sql`; `src/config/database.js`.

| Mã | Trạng thái | Trang/module liên quan | Nội dung yêu cầu gốc | Frontend cần kiểm tra/sửa | Backend/API cần kiểm tra/sửa | Database/migration/seed liên quan | Cách kiểm thử |
|---|---|---|---|---|---|---|---|
| REQ-PROVINCE-001 | [x] | /admin/locations/provinces | Thiếu tìm kiếm theo loại; cần select bên cạnh ô tìm kiếm để chọn tìm theo mã, tên hoặc loại tỉnh/thành phố. | `locations-provinces.pug`, `locations.js` có select `#province-search-field`, giữ query khi lọc/phân trang. | `viewController.adminLocationProvinces`, `locationController.getAllProvinces` nhận `searchField` và lọc theo `MaTinh`, `TenTinh`, `LoaiTinh`. | Không cần DB mới; dùng `TINH.LoaiTinh`. | Đã kiểm tra `node --check`, Pug compile; search field được truyền qua query. |
| REQ-PROVINCE-002 | [x] | /admin/locations/provinces | Khi thêm tỉnh/thành phố không nên để nhập mã tỉnh; mã tỉnh tự tăng dần; ô mã bôi xám để hiểu không thể sửa. | Form thêm hiển thị mã readonly `Tự động khi lưu`, style xám. | `locationController.createProvince` tự sinh `MaTinh` bằng `getNextNumericCode`, không dùng payload `MaTinh`. | Không cần migration; `TINH.MaTinh` dạng chuỗi numeric hiện có. | Đã kiểm tra code sinh mã và `node --check`. |
| REQ-PROVINCE-003 | [x] | /admin/locations/provinces | Thiếu thao tác xóa tỉnh/thành phố; phải kiểm tra ràng buộc tới bảng khác khi xóa tỉnh. | Bảng có nút xóa và confirm, báo lỗi từ API. | `locationController.deleteProvince` kiểm tra `PHUONGXA` chưa xóa trước khi soft delete. | Dùng soft delete `DaXoa`, không cần migration. | Đã kiểm tra route DELETE và controller syntax. |
| REQ-PROVINCE-004 | [x] | /admin/locations/provinces | Khi sửa tỉnh/thành phố không được sửa mã tỉnh; các ô không thể sửa phải tô xám và hiện thông báo khi click. | Form sửa set mã readonly, class `readonly-control`, click gọi `notifyReadonlyField`. | `locationController.updateProvince` chặn đổi `MaTinh` bằng `rejectCodeChange`. | Không cần DB mới. | Đã kiểm tra `node --check`; click readonly có message qua JS. |
| REQ-PROVINCE-005 | [x] | /admin/locations/provinces | Thiếu cột sửa bởi ai, sửa vào thời điểm đầy đủ giờ phút giây, ngày tháng năm. | Bảng/detail có cột `Sửa bởi`, `Sửa lúc`, format đủ giây. | `locationController` ghi `updateAudit`; `viewController`/API attach `NguoiCapNhatTen`. | `TINH.NguoiCapNhat`, `TINH.NgayCapNhat` có trong schema/init/bootstrap. | Đã chạy `npx prisma validate` và Pug compile. |
| REQ-PROVINCE-006 | [x] | /admin/locations/provinces | Click vào bản ghi tỉnh/thành phố thì hiện chi tiết thông tin tỉnh/thành phố đó, chỉ xem. | Dòng bảng có `data-record`, click mở `location-detail-modal`; nút thao tác chặn bubbling. | List trả đủ dữ liệu cho detail. | Không cần DB mới. | Đã compile Pug và kiểm tra JS detail modal. |
| REQ-PROVINCE-007 | [x] | /admin/locations/provinces | Xóa cột ngày tạo. | Bảng không còn cột `Ngày tạo`, colspan cập nhật. | View không dùng `NgayTao` cho bảng. | Không cần DB migration. | Đã compile `locations-provinces.pug`. |
| REQ-PROVINCE-008 | [x] | /admin/locations/provinces | Hiệu chỉnh lại giao diện thân thiện với người dùng sau các thao tác trên. | Toolbar search/filter, bảng, modal thêm/sửa/detail và confirm xóa dùng style địa danh thống nhất. | API trả message rõ cho create/update/delete. | Không cần DB mới. | Đã chạy `node --check`, Pug compile, `npx prisma validate`. |

## 2. Trang phường/xã

File đã sửa/kiểm tra trong nhóm: `src/views/pages/admin/locations-wards.pug`; `src/public/js/admin/locations.js`; `src/public/css/admin/locations.css`; `src/controllers/viewController.js`; `src/controllers/locationController.js`; `src/routes/locationRoutes.js`; `prisma/schema.prisma`; `src/config/init.sql`; `src/config/database.js`.

| Mã | Trạng thái | Trang/module liên quan | Nội dung yêu cầu gốc | Frontend cần kiểm tra/sửa | Backend/API cần kiểm tra/sửa | Database/migration/seed liên quan | Cách kiểm thử |
|---|---|---|---|---|---|---|---|
| REQ-WARD-001 | [x] | /admin/locations/wards | Thiếu tìm kiếm theo loại; cần select bên cạnh ô tìm kiếm để chọn tìm theo mã, tên hoặc loại phường/xã. | `locations-wards.pug`, `locations.js` có select `#ward-search-field`. | `viewController.adminLocationWards`, `locationController.getAllWards` nhận `searchField`, lọc theo `MaPhuongXa`, `TenPhuongXa`, `Loai`. | Không cần DB mới. | Đã chạy `node --check` và Pug compile. |
| REQ-WARD-002 | [x] | /admin/locations/wards | Chưa có lọc theo loại phường/xã. | Có filter `#ward-filter-type` với `Phường`, `Xã`, `Tất cả`. | API/list nhận `Loai`, lọc đúng whitelist. | Constraint/seed chỉ dùng `Phường`, `Xã`; `npx prisma validate` pass. | Đã kiểm tra code filter và compile Pug. |
| REQ-WARD-003 | [x] | /admin/locations/wards | Thiếu cột sửa bởi ai, sửa vào thời điểm đầy đủ giờ phút giây, ngày tháng năm. | Bảng/detail có `Sửa bởi`, `Sửa lúc` đủ giây. | `locationController` ghi audit; `viewController` attach `NguoiCapNhatTen`. | `PHUONGXA.NguoiCapNhat`, `PHUONGXA.NgayCapNhat` có trong schema/init/bootstrap. | Đã chạy `npx prisma validate` và Pug compile. |
| REQ-WARD-004 | [x] | /admin/locations/wards | Thiếu thao tác xóa phường/xã. | Bảng có nút xóa, confirm và hiển thị lỗi API. | `locationController.deleteWard` kiểm tra sinh viên liên quan trước khi soft delete. | Dùng soft delete `DaXoa`, không cần migration. | Đã kiểm tra route DELETE và controller syntax. |
| REQ-WARD-005 | [x] | /admin/locations/wards | Khi sửa hay thêm phường/xã không được sửa/nhập mã; mã tự tăng; ô mã tô xám và click hiện thông báo. | Form thêm/sửa mã readonly, style xám, click gọi `notifyReadonlyField`. | `createWard` tự sinh `MaPhuongXa`; `updateWard` chặn đổi mã. | Không cần migration; mã dạng chuỗi numeric hiện có. | Đã kiểm tra `node --check`. |
| REQ-WARD-006 | [x] | /admin/locations/wards | Loại phường/xã mặc định chỉ có hai loại là Phường hoặc Xã, không có Thị trấn. | Form dùng select chỉ gồm `Phường`, `Xã`. | `locationController.createWard/updateWard` validate `WARD_TYPES`. | Constraint/seed chỉ cho `Phường`, `Xã`; `npx prisma validate` pass. | Đã kiểm tra code whitelist và Pug compile. |
| REQ-WARD-007 | [x] | /admin/locations/wards | Click vào bản ghi phường/xã thì hiện chi tiết thông tin phường/xã đó, chỉ xem. | Dòng bảng có `data-record`, click mở `location-detail-modal`; nút thao tác chặn bubbling. | List trả đủ thông tin phường/xã, tỉnh và audit. | Không cần DB mới. | Đã compile Pug và kiểm tra JS detail modal. |
| REQ-WARD-008 | [x] | /admin/locations/wards | Xóa cột ngày tạo. | Bảng không còn cột `Ngày tạo`, colspan cập nhật. | View không dùng `NgayTao` cho bảng. | Không cần DB migration. | Đã compile `locations-wards.pug`. |
| REQ-WARD-009 | [x] | /admin/locations/wards | Hiệu chỉnh lại giao diện thân thiện với người dùng sau các thao tác trên. | Toolbar search/filter, bảng, modal form/detail và confirm xóa dùng style địa danh thống nhất. | API trả message rõ cho create/update/delete. | Không cần DB mới ngoài các mục trên. | Đã chạy `node --check`, Pug compile, `npx prisma validate`. |

## 3. Trang sinh viên

File đã sửa trong nhóm: Chưa sửa code trong lượt này; chỉ bổ sung checklist. File dự kiến cần kiểm tra nếu có yêu cầu chi tiết: `src/views/pages/admin/students.pug`; `src/public/js/admin/students.js`; `src/controllers/studentController.js`; `src/controllers/viewController.js`; `src/routes/studentRoutes.js`; `prisma/schema.prisma`.

| Mã | Trạng thái | Trang/module liên quan | Nội dung yêu cầu gốc | Frontend cần kiểm tra/sửa | Backend/API cần kiểm tra/sửa | Database/migration/seed liên quan | Cách kiểm thử |
|---|---|---|---|---|---|---|---|
| REQ-STUDENT-ADMIN-001 | [ ] | /admin/students | File yêu cầu gốc có section `Trang sinh viên` nhưng không ghi nội dung chi tiết bên dưới. Giả định: chưa có yêu cầu triển khai cụ thể, cần giữ lại trong checklist để không bỏ sót section. | Chưa xác định thay đổi; khi có mô tả cần rà `students.pug`/`students.js`. | Chưa xác định thay đổi; khi có mô tả cần rà `studentController`/`viewController`. | Chưa xác định; phụ thuộc yêu cầu bổ sung. | Xác nhận lại với yêu cầu gốc/cập nhật sau; hiện chỉ kiểm tra rằng section đã được ghi nhận. |

## 4. Trang môn học

File đã sửa/kiểm tra trong nhóm: `src/views/pages/admin/courses.pug`; `src/public/js/admin/courses.js`; `src/public/css/admin/course-tools.css`; `src/controllers/viewController.js`; `src/controllers/courseController.js`; `src/routes/courseRoutes.js`; `src/models/courseModel.js`; `prisma/schema.prisma`; `src/config/init.sql`; `src/config/database.js`.

| Mã | Trạng thái | Trang/module liên quan | Nội dung yêu cầu gốc | Frontend cần kiểm tra/sửa | Backend/API cần kiểm tra/sửa | Database/migration/seed liên quan | Cách kiểm thử |
|---|---|---|---|---|---|---|---|
| REQ-COURSE-001 | [x] | /admin/courses | Bên cạnh ô tìm kiếm có select để tìm theo mã, tên, loại môn học, khoa. | `courses.pug` có `#course-search-field`; `courses.js` giữ query search/filter. | `viewController.adminCourses`, `courseController.getAllCourses/exportCourses` lọc theo `searchField`. | Không cần DB mới; dùng `MONHOC` join `KHOA`. | Đã chạy `node --check`; Pug compile; search theo field có query tương ứng. |
| REQ-COURSE-002 | [x] | /admin/courses | Ở chỗ sửa môn học, các ô không thể sửa phải tô xám và hiện thông báo khi nhấn. | Form sửa khóa `MaMonHoc`, `SoTinChi` bằng `.lockable-field`, click/focus hiện toast. | `courseController.updateCourse` chặn đổi `MaMonHoc` và `SoTinChi`. | Không cần DB mới. | Đã chạy `node --check`; backend trả 400 nếu cố đổi mã/tín chỉ. |
| REQ-COURSE-003 | [x] | /admin/courses | Ô số tín chỉ không được sửa; khi nhấn hiển thị thông báo số tín chỉ dựa vào số tiết và loại môn. | `SoTinChi` readonly, tự sync theo `SoTiet` và `LoaiMon`, message nêu rõ quy tắc. | Backend không nhận sửa trực tiếp `SoTinChi`; import cũng validate tín chỉ theo công thức. | `MONHOC.SoTinChi` được tính theo số tiết/loại môn trong schema/init/bootstrap. | Đã chạy `npx prisma validate`, `node --check`. |
| REQ-COURSE-004 | [x] | /admin/courses | Click bản ghi môn học chỉ hiện thông tin chi tiết môn học đó, không cần môn điều kiện, chương trình đào tạo hay lớp đã mở. | Click dòng mở `course-detail-modal` chỉ render thông tin môn học cơ bản. | `getCourseById` chỉ include `KHOA`, không include prerequisite/CTDT/lớp mở. | Không cần DB mới. | Đã compile Pug và kiểm tra JS detail modal. |
| REQ-COURSE-005 | [x] | /admin/courses | Có chức năng nhập Excel các cột tương ứng như xuất Excel. | Modal/nút nhập Excel dùng format cùng export. | `courseController.importCourses`, route `/api/courses/import` đọc `.xls/.xlsx`, validate từng dòng. | Không cần migration; dùng cột `MONHOC` hiện có. | Đã chạy `node --check`; import route có multer memory + validate header. |
| REQ-COURSE-006 | [x] | /admin/courses | Khi nhấn nhập Excel phải có hướng dẫn format file upload. | Modal import hiển thị `MaMonHoc, TenMonHoc, LoaiMon, SoTinChi, SoTiet, Khoa, TrangThai`. | Không cần API metadata riêng. | Không cần DB mới. | Đã compile `courses.pug`. |
| REQ-COURSE-007 | [x] | /admin/courses | Khi nhập Excel phải hiện kết quả bản ghi nào thành công, bản ghi nào thất bại và lỗi cụ thể. | `renderCourseImportResults` render từng dòng thành công/thất bại và message lỗi. | `importCourses` trả `rows`, `successCount`, `errorCount`, message từng dòng. | Không cần DB mới. | Đã chạy `node --check`; import xử lý dòng đúng/sai riêng. |
| REQ-COURSE-008 | [x] | /admin/courses | Hiệu chỉnh lại giao diện thân thiện với người dùng sau các thao tác trên. | Toolbar, bảng, modal detail/edit/import dùng style `course-tools.css`. | API search/update/import trả message rõ. | Không cần DB mới ngoài các mục trên. | Đã chạy `node --check`, Pug compile, `npx prisma validate`. |

## 5. Trang môn học mở

File đã sửa/kiểm tra trong nhóm: `src/views/pages/admin/open-courses.pug`; `src/public/js/admin/open-courses.js`; `src/public/css/admin/open-courses.css`; `src/controllers/viewController.js`; `src/controllers/openCourseController.js`; `src/routes/openCourseRoutes.js`; `prisma/schema.prisma`.

| Mã | Trạng thái | Trang/module liên quan | Nội dung yêu cầu gốc | Frontend cần kiểm tra/sửa | Backend/API cần kiểm tra/sửa | Database/migration/seed liên quan | Cách kiểm thử |
|---|---|---|---|---|---|---|---|
| REQ-OPENCOURSE-001 | [x] | /admin/open-courses | Có select chọn tìm kiếm theo mã môn hoặc tên môn bên trái ô tìm kiếm. | `open-courses.pug` có `#oc-search-field`; `open-courses.js` gửi `searchField` cùng keyword/filter. | `viewController.adminOpenCourses`, `openCourseController.buildOpenCourseWhere` lọc theo `MONHOCMO.MaMonHoc` hoặc `MONHOC.TenMonHoc`. | Không cần DB mới; dùng join `MONHOC`. | Đã chạy `node --check`, Pug compile; chọn mã/tên môn tạo query `searchField` đúng. |
| REQ-OPENCOURSE-002 | [x] | /admin/open-courses | Thêm cột mã môn, tách ra từ cột môn học. | Bảng có cột `Mã môn` riêng và cell `.open-course-code-cell`. | Query list/detail trả `MaMonHoc` riêng. | Không cần DB mới. | Đã compile Pug; mở bảng thấy mã môn ở cột riêng. |
| REQ-OPENCOURSE-003 | [x] | /admin/open-courses | Thêm cột loại môn, tách ra từ cột môn học. | Bảng có cột `Loại môn` riêng. | Query list/detail trả `MONHOC.LoaiMon`. | Không cần DB mới. | Đã compile Pug; loại môn lấy từ `MONHOC.LoaiMon`. |
| REQ-OPENCOURSE-004 | [x] | /admin/open-courses | Bỏ cột lớp mở. | Bảng không còn cột lớp mở, chỉ giữ thông tin này trong detail nếu cần tham khảo. | Không cần API mới; list vẫn có thể trả `SoLopMo` để detail và kiểm tra ràng buộc. | Không cần DB mới. | Đã compile Pug; header/body không còn cột lớp mở. |
| REQ-OPENCOURSE-005 | [x] | /admin/open-courses | Bỏ cột tín chỉ. | Bảng không còn cột tín chỉ, chỉ giữ tín chỉ trong detail. | Không cần API mới; list vẫn trả `SoTinChi` phục vụ detail. | Không cần DB mới. | Đã compile Pug; header/body không còn cột tín chỉ. |
| REQ-OPENCOURSE-006 | [x] | /admin/open-courses | Mặc định khi vào trang, select lọc học kỳ chọn học kỳ gần nhất. | Filter học kỳ nhận `filterHocKy` từ view khi không có query. | `viewController.adminOpenCourses` chọn `HOCKY` gần nhất theo `NgayBatDau desc`, `MaHocKy desc` và lọc mặc định. | Không cần DB mới; dựa trên `HOCKY`/`NAMHOC`. | Đã kiểm tra code default; vào trang không query sẽ chọn kỳ gần nhất, `MaHocKy=all` mới bỏ lọc. |
| REQ-OPENCOURSE-007 | [x] | /admin/open-courses | Khi thêm môn học mở, select môn học đổi thành popup có tìm kiếm theo mã môn hoặc tên môn. | Form thêm/sửa dùng hidden input + `#open-course-picker-modal`, không dùng select dài. | Route `/api/open-courses/available` tìm môn theo mã/tên và loại trừ môn đã mở trong học kỳ. | Không cần DB mới. | Đã chạy `node --check`; mở modal, chọn học kỳ, tìm/chọn môn rồi lưu. |
| REQ-OPENCOURSE-008 | [x] | /admin/open-courses | Click dòng bản ghi thì popup thông tin chi tiết bản ghi đó. | Dòng `.open-course-row` gọi `openOpenCourseDetail`, action cell chặn bubbling. | Route `/api/open-courses/:id` trả detail qua `getOpenCourseRow`. | Không cần DB mới. | Đã chạy `node --check`; click dòng mở detail đúng, click sửa/xóa không mở detail. |
| REQ-OPENCOURSE-009 | [x] | /admin/open-courses | Hiệu chỉnh giao diện thân thiện với người dùng sau các thao tác trên. | Toolbar search/filter, bảng tách cột, popup chọn môn và detail modal dùng style `open-courses.css`/`course-tools.css`. | API search/create/update/delete trả message rõ và kiểm tra duplicate/lớp đang mở. | Không cần DB mới ngoài các mục trên. | Đã chạy `node --check`, Pug compile, rà route/sidebar/permission không cần đổi. |

## 6. Trang ràng buộc môn học

File đã sửa/kiểm tra trong nhóm: `src/views/pages/admin/prerequisites.pug`; `src/public/js/admin/prerequisites.js`; `src/public/css/admin/course-tools.css`; `src/controllers/viewController.js`; `src/controllers/prerequisiteController.js`; `src/routes/prerequisiteRoutes.js`; `prisma/schema.prisma`.

| Mã | Trạng thái | Trang/module liên quan | Nội dung yêu cầu gốc | Frontend cần kiểm tra/sửa | Backend/API cần kiểm tra/sửa | Database/migration/seed liên quan | Cách kiểm thử |
|---|---|---|---|---|---|---|---|
| REQ-PREREQ-001 | [x] | /admin/prerequisites | Bỏ cái xem sơ đồ. | `prerequisites.pug`/`prerequisites.js` không còn nút hoặc khu vực xem sơ đồ. | Đã gỡ route `/api/prerequisites/graph/data` và export `getPrerequisiteGraph` khỏi `prerequisiteController`. | Không cần DB mới. | Đã chạy `node --check`, Pug compile; `rg` không còn endpoint graph. |
| REQ-PREREQ-002 | [x] | /admin/prerequisites | Thêm select bên trái ô tìm kiếm để tìm theo mã, tên môn học hoặc tên môn điều kiện. | `#prereq-search-field` nằm cạnh ô tìm kiếm, gửi `searchField`. | `viewController.adminPrerequisites`, `prerequisiteController.getPrerequisites` lọc theo mã, tên môn chính, tên môn điều kiện. | Không cần DB mới; dùng `DIEUKIENMONHOC` join `MONHOC`. | Đã chạy `node --check`, Pug compile; kiểm tra query `searchField=code/courseName/requiredName`. |
| REQ-PREREQ-003 | [x] | /admin/prerequisites | Khi sửa/thêm ràng buộc, chọn môn học hoặc môn điều kiện bằng popup tìm kiếm mã môn hoặc tên môn, thay cho select dài. | Form thêm/sửa dùng hidden input + `#prereq-course-picker-modal` cho môn chính và môn điều kiện. | Picker dùng `/api/courses?all=true&searchField=all`; create/update nhận `MaMonHoc`, `MaMonDieuKien` và validate trùng/loại điều kiện. | Không cần DB mới. | Đã chạy `node --check`; thao tác thêm/sửa chọn hai môn bằng popup và lưu. |
| REQ-PREREQ-004 | [x] | /admin/prerequisites | Click dòng bản ghi thì popup thông tin chi tiết bản ghi đó. | Dòng `.prereq-row` gọi `openPrereqDetail`, cell thao tác chặn bubbling. | Route `/api/prerequisites/:id` trả môn chính, môn điều kiện, mô tả, trạng thái. | Không cần DB mới. | Đã chạy `node --check`; click dòng mở detail, click sửa/xóa không mở detail. |
| REQ-PREREQ-005 | [x] | /admin/prerequisites | Hiệu chỉnh giao diện thân thiện với người dùng sau các thao tác trên. | Toolbar, bảng, popup chọn môn, modal thêm/sửa/detail dùng style admin chung và `course-tools.css`. | API trả lỗi duplicate, môn trùng, môn không tồn tại, loại điều kiện không hợp lệ. | Đã rà ràng buộc unique/FK qua Prisma; không cần migration. | Đã chạy `node --check`, Pug compile; ràng buộc môn trùng hoặc thiếu môn trả lỗi cụ thể. |

## 8. Trang quản lý phòng học

File đã sửa trong nhóm: `prisma/schema.prisma`; `src/config/init.sql`; `src/config/database.js`; `src/controllers/viewController.js`; `src/controllers/roomController.js`; `src/routes/roomRoutes.js`; `src/views/pages/admin/rooms.pug`; `src/public/js/admin/rooms.js`.

| Mã | Trạng thái | Trang/module liên quan | Nội dung yêu cầu gốc | Frontend cần kiểm tra/sửa | Backend/API cần kiểm tra/sửa | Database/migration/seed liên quan | Cách kiểm thử |
|---|---|---|---|---|---|---|---|
| REQ-ROOM-001 | [x] | /admin/rooms | Thiếu tham chiếu và dữ liệu tới học kỳ; tạo lại bảng phòng học và seed database phòng học cho từng kỳ. | Danh sách, form thêm/sửa và popup dùng `MaHocKy`. | `roomController.getRoomRows` lọc theo học kỳ; tạo/sửa upsert phân bổ học kỳ. | Thêm `PHONGHOCHOCKY`, FK tới `PHONGHOC` và `HOCKY`, seed/backfill phòng theo từng kỳ trong `init.sql` và bootstrap. | Đã kiểm tra schema/init/bootstrap; `npx prisma validate`; Pug compile; JS syntax. |
| REQ-ROOM-002 | [x] | /admin/rooms | Thêm select bên trái ô tìm kiếm để tìm theo mã phòng, tên phòng, tòa nhà, loại phòng hoặc học kỳ. | `rooms.js` tích hợp `AdminUI.createSearchCriterionControl`. | `roomController` nhận `searchField` và query raw theo từng trường. | Dùng join `PHONGHOCHOCKY`/`HOCKY` để tìm theo học kỳ. | Chọn từng tiêu chí và gửi query `searchField` tương ứng. |
| REQ-ROOM-003 | [x] | /admin/rooms | Thêm filter lọc theo status đang dùng và filter học kỳ. | Thêm `#filter-semester` và `#filter-used-status`. | `getRoomRows` tính `ClassCount`, `IsInUse`, lọc `usedStatus=in_use/free`. | Dựa trên `LOPMO` + `LICHHOCLOP` theo học kỳ. | Lọc phòng đang có lớp/chưa có lớp theo học kỳ. |
| REQ-ROOM-004 | [x] | /admin/rooms | Bỏ cột đang dùng; trong cột thao tác có nút Lớp đang dùng mở popup danh sách lớp đang dùng phòng trong học kỳ, mặc định học kỳ hiện tại. | Bảng bỏ cột đang dùng; nút `Lớp đang dùng` nằm trong cột thao tác. | Thêm `GET /api/rooms/:id/classes`. | Query lớp dùng phòng theo `MaHocKy`. | Mở popup, đổi học kỳ trong popup, kiểm tra danh sách lớp. |
| REQ-ROOM-005 | [x] | /admin/rooms | Thêm cột học kỳ. | Bảng hiển thị `HocKyLabel`/`MaHocKy`. | `getRoomRows` trả `MaHocKy`, `TenHocKy`, `TenNamHoc`. | Phụ thuộc `PHONGHOCHOCKY`. | Danh sách đổi theo filter học kỳ. |
| REQ-ROOM-006 | [x] | /admin/rooms | Click dòng bản ghi thì popup thông tin chi tiết bản ghi đó. | `rooms.pug` thêm `data-record`; `rooms.js` gắn `AdminUI.attachRowDetailHandlers`. | Dữ liệu list đủ để render chi tiết. | Không cần thêm DB ngoài học kỳ. | Click dòng, không click nút thao tác, modal chi tiết mở đúng. |
| REQ-ROOM-007 | [x] | /admin/rooms | Khi sửa phòng học, trường không được phép sửa phải bôi xám và hiện thông báo lỗi khi click. | `MaPhong` readonly, có `data-readonly-message`, dùng `AdminUI.initReadonlyNotices`. | `updateRoom` chặn đổi `MaPhong`. | Không cần thêm DB. | Mở sửa, click mã phòng, kiểm tra toast; gửi API đổi mã bị chặn. |
| REQ-ROOM-008 | [x] | /admin/rooms | Hiệu chỉnh giao diện thân thiện sau các thao tác trên. | Bố trí filter, bảng, modal form và modal lớp dùng chung style admin. | Không thêm API ngoài popup lớp. | Không cần thêm DB. | Pug compile và kiểm tra tương tác chính. |

## 9. Trang quản lý giảng viên

File đã sửa trong nhóm: `prisma/schema.prisma`; `src/config/init.sql`; `src/config/database.js`; `src/controllers/viewController.js`; `src/controllers/lecturerController.js`; `src/routes/lecturerRoutes.js`; `src/views/pages/admin/lecturers.pug`; `src/public/js/admin/lecturers.js`.

| Mã | Trạng thái | Trang/module liên quan | Nội dung yêu cầu gốc | Frontend cần kiểm tra/sửa | Backend/API cần kiểm tra/sửa | Database/migration/seed liên quan | Cách kiểm thử |
|---|---|---|---|---|---|---|---|
| REQ-LECTURER-001 | [x] | /admin/lecturers | Thiếu tham chiếu và dữ liệu tới học kỳ; tạo lại bảng giảng viên và seed giảng viên cho từng kỳ. | Danh sách, form và popup dùng `MaHocKy`. | `lecturerController.getLecturerRows` lọc theo học kỳ; tạo/sửa upsert phân bổ học kỳ. | Thêm `GIANGVIENHOCKY`, FK tới `GIANGVIEN` và `HOCKY`, seed/backfill giảng viên theo từng kỳ. | Đã kiểm tra schema/init/bootstrap; `npx prisma validate`; Pug compile; JS syntax. |
| REQ-LECTURER-002 | [x] | /admin/lecturers | Tách học hàm và học vị khỏi trường hiện tại. | Bảng/form dùng `HocHam` và `HocVi` riêng. | Controller lưu `HocHam`, `HocVi`, đồng bộ `HocHamHocVi` legacy. | Thêm cột `GIANGVIEN.HocHam`, `GIANGVIEN.HocVi`, backfill từ legacy. | Thêm/sửa lưu riêng hai trường. |
| REQ-LECTURER-003 | [x] | /admin/lecturers | Trong thêm/sửa giảng viên, học hàm và học vị phải là select với option phù hợp, không nhập tự do. | Form dùng select học hàm `GS/PGS` và học vị `CN/KS/ThS/TS`. | Controller validate whitelist giá trị. | Không cần bảng danh mục riêng. | Gửi giá trị ngoài whitelist qua API bị chặn. |
| REQ-LECTURER-004 | [x] | /admin/lecturers | Thêm select bên trái ô tìm kiếm để tìm theo mã GV, họ tên, khoa, email giảng viên hoặc học kỳ. | `lecturers.js` tích hợp `AdminUI.createSearchCriterionControl`. | Query theo `searchField` qua `getLecturerRows`. | Dùng join `GIANGVIENHOCKY`/`HOCKY`/`KHOA`. | Chọn từng tiêu chí và kiểm tra query tương ứng. |
| REQ-LECTURER-005 | [x] | /admin/lecturers | Thiếu data email giảng viên; seed database thêm email, không được thiếu. | Email hiển thị ở bảng và bắt buộc trong form. | Controller validate email khi thêm/sửa. | `GIANGVIEN.Email` là NOT NULL; seed/backfill `lower(MaGiangVien) || '@uit.edu.vn'`. | Kiểm tra seed/bootstrap không còn email trống. |
| REQ-LECTURER-006 | [x] | /admin/lecturers | Bỏ cột số lớp; thay bằng nút xem lớp trong cột thao tác, popup lớp giảng viên đang dạy trong học kỳ tùy chọn, mặc định học kỳ hiện tại. | Nút `Xem lớp` mở modal theo học kỳ. | Thêm `GET /api/lecturers/:id/classes`. | Query `LOPMO` theo `MaGiangVien` và `MaHocKy`. | Mở popup, đổi học kỳ, kiểm tra lớp có/không có dữ liệu. |
| REQ-LECTURER-007 | [x] | /admin/lecturers | Thêm cột học hàm, học vị trước họ tên hoặc gộp rõ với cột họ tên để tránh quá rộng. | Bảng có cột `Học hàm`, `Học vị` trước `Họ tên`. | `getLecturerRows` trả riêng `HocHam`, `HocVi`. | Phụ thuộc backfill tách trường. | Bảng hiển thị rõ hai trường. |
| REQ-LECTURER-008 | [x] | /admin/lecturers | Click dòng bản ghi thì popup thông tin chi tiết bản ghi đó. | `lecturers.pug` thêm `data-record`; `lecturers.js` gắn detail modal. | Dữ liệu list đủ để render chi tiết. | Không cần thêm DB ngoài học kỳ/tách trường. | Click dòng mở modal chi tiết chỉ xem. |
| REQ-LECTURER-009 | [x] | /admin/lecturers | Khi sửa giảng viên, trường không được phép sửa phải bôi xám và hiện thông báo lỗi khi click. | `MaGiangVien` readonly, có `data-readonly-message`, dùng `AdminUI.initReadonlyNotices`. | `updateLecturer` chặn đổi `MaGiangVien`. | Không cần thêm DB. | Mở sửa, click mã GV, kiểm tra toast; gửi API đổi mã bị chặn. |
| REQ-LECTURER-010 | [x] | /admin/lecturers | Hiệu chỉnh giao diện thân thiện sau các thao tác trên. | Bố trí filter, bảng, form select, popup lớp và modal chi tiết theo style admin. | Không thêm API ngoài popup lớp. | Không cần thêm DB. | Pug compile và kiểm tra tương tác chính. |

## 10. Người dùng, hồ sơ quản trị viên, phân quyền hệ thống

File đã sửa/kiểm tra trong nhóm: `prisma/schema.prisma`; `src/config/init.sql`; `src/config/database.js`; `src/controllers/viewController.js`; `src/controllers/roleController.js`; `src/controllers/authController.js`; `src/routes/roleRoutes.js`; `src/routes/authRoutes.js`; `src/views/pages/admin/users.pug`; `src/views/pages/admin/profile.pug`; `src/views/pages/admin/permissions.pug`; `src/public/js/admin/users.js`; `src/public/js/admin/profile.js`; `src/public/js/admin/permissions.js`; `src/public/css/theme.css`; `src/utils/permissionCatalog.js`.

| Mã | Trạng thái | Trang/module liên quan | Nội dung yêu cầu gốc | Frontend cần kiểm tra/sửa | Backend/API cần kiểm tra/sửa | Database/migration/seed liên quan | Cách kiểm thử |
|---|---|---|---|---|---|---|---|
| REQ-USER-001 | [x] | /admin/users | Thêm select bên trái ô tìm kiếm theo tên đăng nhập, họ tên, email, mã sinh viên. | `users.pug` có `#search-field`; `users.js` gửi `searchField`. | `viewController.adminUsers` và `roleController.getAllAccounts` lọc theo trường tương ứng. | Không cần DB mới. | Chọn từng tiêu chí và kiểm tra URL/API có `searchField`. |
| REQ-USER-002 | [x] | /admin/users | Tạo tài khoản phải có ô xác nhận mật khẩu. | Modal tạo tài khoản có `#acc-password-confirm`; JS kiểm tra khớp trước khi gửi. | `createAccount` nhận `passwordConfirm` và từ chối nếu không khớp. | Không cần DB mới. | Nhập mật khẩu lệch nhau phải báo lỗi; nhập khớp tạo được. |
| REQ-USER-003 | [x] | /admin/users | Tạo tài khoản hàng loạt cho sinh viên bằng cách lọc sinh viên hiện có theo khoa/ngành, bỏ danh sách MSSV nhập tay. | Modal hàng loạt chỉ còn filter khoa/ngành và kết quả tạo. | `batchCreateStudentAccounts` chỉ nhận `MaKhoa`/`MaNganh`, tìm sinh viên chưa có tài khoản. | Không cần DB mới ngoài bảng mật khẩu tạm. | Chọn khoa hoặc ngành rồi tạo; không còn textarea MSSV/mật khẩu mặc định. |
| REQ-USER-004 | [x] | /admin/users | Tạo mật khẩu ngẫu nhiên cho sinh viên và gửi Gmail hàng loạt. | Kết quả hiển thị trạng thái gửi Gmail. | Sinh mật khẩu bằng `crypto`; gửi qua `nodemailer` nếu SMTP được cấu hình. | Lưu trạng thái gửi email trong `MATKHAUTAMTAIKHOAN`. | Tạo hàng loạt thấy passwordMode `random`, emailStatus `sent/not_configured/failed`. |
| REQ-USER-005 | [x] | /admin/users | Admin xem được mật khẩu sinh viên nhưng mặc định ẩn, nhấn icon mắt mới hiện. | Bảng kết quả/danh sách tài khoản render password mask và nút mắt. | API trả mật khẩu tạm đã lưu cho admin hệ thống. | `MATKHAUTAMTAIKHOAN.MatKhauTam` lưu mật khẩu tạm. | Mật khẩu ban đầu là `********`, bấm icon mắt mới hiện. |
| REQ-USER-006 | [x] | /admin/users | Sau khi tạo hàng loạt phải hiện danh sách tài khoản và mật khẩu để admin xem. | `#batch-result` render bảng tài khoản/mật khẩu ngay trong modal. | API trả `created` gồm tài khoản, MSSV, mật khẩu tạm, trạng thái email. | Dùng bảng mật khẩu tạm để lưu lịch sử. | Tạo hàng loạt xong không reload và thấy danh sách kết quả. |
| REQ-USER-007 | [x] | /admin/users | Nút tạo tài khoản, tạo TK cho SV, danh sách tài khoản nằm ngang hàng; nút danh sách hiện tài khoản/mật khẩu đã tạo. | Toolbar dùng `.users-toolbar`; modal `credential-list-modal` tải danh sách đã tạo. | Thêm `GET /api/roles/accounts/student-credentials`. | Đọc từ `MATKHAUTAMTAIKHOAN`. | Bấm Danh sách TK thấy các tài khoản/mật khẩu tương ứng sinh viên. |
| REQ-USER-008 | [x] | /admin/users | Gộp thông tin bảng để cột thao tác không tràn; bỏ nút reset mật khẩu. | Bảng gộp tài khoản/MSSV/chức vụ và liên hệ; template không còn nút Reset MK. | API reset cũ không được render từ UI. | Không cần DB mới. | Bảng còn đủ cột chính, thao tác không bị mất nút. |
| REQ-USER-009 | [x] | /admin/users | Hiệu chỉnh UI thân thiện và xóa phần không cần thiết. | Bỏ input mật khẩu mặc định/MSSV cũ; thêm style cho bảng credential và ô gộp. | Luồng hàng loạt chỉ xử lý filter khoa/ngành. | Bảng mật khẩu tạm được bootstrap và seed init. | Pug compile, JS syntax, kiểm tra thao tác chính. |
| REQ-ADMINPROFILE-001 | [x] | /admin/profile | Chỉ cho sửa họ tên, email, số điện thoại, phòng ban; trường khác tô xám và click báo lỗi. | `profile.pug` chỉ để editable các trường được phép; username/chức vụ readonly có `data-readonly-message`. | `authController.updateAdminProfile` whitelist `HoTen`, `Email`, `Sdt`, `PhongBan`. | Không cần DB mới. | Click field xám thấy toast; gửi field ngoài whitelist bị từ chối. |
| REQ-ADMINPROFILE-002 | [x] | /admin/profile | Hiệu chỉnh UI thân thiện. | Nút lưu hồ sơ, readonly notice và load dữ liệu profile thống nhất style admin. | `PUT /api/auth/profile` dispatch admin/student đúng luồng. | Không cần DB mới. | Lưu 4 trường được phép và reload profile. |
| REQ-PERMISSION-001 | [x] | /admin/permissions | Hai nút quyền truy cập và nhóm người dùng dạng toggle/tab. | `permissions.pug` có `.permission-tabs`; JS `switchTab` ẩn/hiện đúng panel. | Không cần API mới. | Không cần DB mới. | Bấm Nhóm người dùng/Quyền truy cập thấy đúng nội dung. |
| REQ-PERMISSION-002 | [x] | /admin/permissions | Kiểm tra thêm/xóa/sửa quyền hoặc nhóm quyền sau khi thêm nhiều mục sidebar. | Catalog có thêm quyền cho sidebar địa danh con. | `permissionCatalog` ánh xạ quyền mới tới route view/API. | Seed `CHUCNANG`/`PHANQUYEN` thêm `ADMIN_LOCATION_PROVINCES`, `ADMIN_LOCATION_WARDS`. | Kiểm tra nhóm ADMIN và ADMIN_DAOTAO có quyền mới. |
| REQ-PERMISSION-003 | [x] | /admin/permissions | Hiệu chỉnh UI thân thiện. | Tab permission dùng style segmented control, giữ trạng thái active rõ. | Không cần API mới. | Không cần DB mới ngoài seed quyền. | Pug compile và JS syntax. |

## 11. Trang quản lý đăng ký môn học

File đã sửa/kiểm tra trong nhóm: `src/views/pages/admin/registrations.pug`; `src/public/js/admin/registrations.js`; `src/public/css/admin/registrations.css`; `src/controllers/viewController.js`; `src/controllers/registrationController.js`; `src/controllers/semesterController.js`; `src/routes/registrationRoutes.js`; `src/routes/semesterRoutes.js`; `src/utils/registrationSearch.js`; `src/utils/registrationStats.js`; `src/utils/registrationWindow.js`; `src/views/pages/admin/reports.pug`; `src/public/js/admin/reports.js`; `src/views/partials/sidebar-admin.pug`; `prisma/schema.prisma`.

| Mã | Trạng thái | Trang/module liên quan | Nội dung yêu cầu gốc | Frontend cần kiểm tra/sửa | Backend/API cần kiểm tra/sửa | Database/migration/seed liên quan | Cách kiểm thử |
|---|---|---|---|---|---|---|---|
| REQ-REGISTRATION-001 | [x] | /admin/registrations | Khi nhấn nút xem trong thao tác, chỉ xem được môn sinh viên đó đăng ký trong học kỳ đó, không xem hết học kỳ. | Nút xem truyền `SoPhieu` của dòng; modal render đúng phiếu và học kỳ. Giả định: `SoPhieu` là khóa của một phiếu thuộc đúng `MaSv + MaHocKy`, tương đương lọc đồng thời sinh viên/học kỳ. | `registrationController.getRegistrationById` lấy `PHIEUDANGKY` theo `SoPhieu`, include `CHITIETDANGKY` của phiếu đó, không dùng helper xem toàn bộ lịch sử sinh viên. | Không cần DB mới; dựa trên `PHIEUDANGKY.SoPhieu`, `MaSv`, `MaHocKy`. | Đã chạy `node --check`, Pug compile; chọn sinh viên có nhiều học kỳ, bấm từng phiếu thì chỉ thấy môn của phiếu/học kỳ đó. |
| REQ-REGISTRATION-002 | [x] | /admin/registrations, /admin/appeals | Trang đăng ký môn học không nên có đơn chờ duyệt; đơn chờ duyệt thuộc quản lý cứu xét. | `registrations.pug` không còn panel/badge/nút đơn chờ duyệt; trang chỉ còn filter, panel thời hạn và bảng phiếu. | `viewController.adminRegistrations` không query đơn cứu xét; đơn cứu xét xử lý riêng ở `appealController` và `/admin/appeals`. | Không cần DB mới. | Đã compile Pug; mở `/admin/registrations` không còn khối đơn chờ duyệt, `/admin/appeals` vẫn là trang quản lý đơn. |
| REQ-REGISTRATION-003 | [x] | /admin/registrations, /admin/semesters | Không thể chốt đăng ký khi hết hạn để chuyển qua thời gian đóng học phí; cần kiểm tra dữ liệu/ràng buộc và sửa nếu lỗi. | Panel đăng ký hiển thị hạn đăng ký, nút chốt và trạng thái `Thu học phí`; toast chốt báo đã mở thu học phí. | `semesterController.finalizeRegistration` kiểm tra hết hạn qua `assertCanFinalizeRegistration`, đóng lớp dưới ngưỡng, cập nhật `NgayChotDangKy`, `MoThuHocPhi=true`, `NgayMoThuHocPhi`; `registrationWindow.getAppealWindowState` đóng cứu xét khi đã chốt/mở thu. | Không cần migration; `HOCKY.NgayChotDangKy`, `MoThuHocPhi`, `NgayMoThuHocPhi`, `HanDongHocPhi` đã có trong Prisma/init/bootstrap. | Đã chạy `node --check`, Pug compile; chốt học kỳ hết hạn đăng ký phải chuyển sang thu học phí và lỗi trả message cụ thể nếu chưa đủ điều kiện. |
| REQ-REGISTRATION-004 | [x] | /admin/registrations, /admin/reports | Chuyển thống kê đăng ký môn học và xuất thống kê Excel sang sidebar thống kê. | `registrations.pug` không còn thống kê/export; `reports.pug` có section `#registration-statistics`; sidebar có link `Thống kê đăng ký`. | API thống kê/export vẫn dùng `/api/registrations/stats` và `/api/registrations/export` nhưng chỉ được gọi từ trang báo cáo; không cần route DB mới. | Không cần DB mới; dùng lại dữ liệu đăng ký hiện có. | Đã rà `reports.js` gọi stats/export; sidebar trỏ `/admin/reports#registration-statistics`; Pug compile đăng ký pass. |
| REQ-REGISTRATION-005 | [x] | /admin/registrations | Hiệu chỉnh giao diện thân thiện với người dùng sau các thao tác trên. | Search/filter, bảng, modal detail, panel hạn đăng ký và trạng thái mở thu dùng layout hiện có; thêm trạng thái thu học phí rõ sau khi chọn học kỳ. | Response lỗi xem chi tiết/chốt đăng ký trả message cụ thể; chốt đăng ký trả summary lớp mở/lớp đóng/đăng ký hủy. | Không cần DB mới ngoài các trường học kỳ đã có. | Đã chạy `node --check`, Pug compile; rà xem chi tiết, chốt đăng ký, reset filter và trạng thái sau reload. |

## 12. Trang quản lý đơn cứu xét

File đã sửa/kiểm tra trong nhóm: `src/views/pages/admin/appeals.pug`; `src/public/js/admin/appeals.js`; `src/controllers/viewController.js`; `src/controllers/appealController.js`; `src/routes/appealRoutes.js`; `src/utils/registrationWindow.js`; `prisma/schema.prisma`.

| Mã | Trạng thái | Trang/module liên quan | Nội dung yêu cầu gốc | Frontend cần kiểm tra/sửa | Backend/API cần kiểm tra/sửa | Database/migration/seed liên quan | Cách kiểm thử |
|---|---|---|---|---|---|---|---|
| REQ-APPEAL-001 | [x] | /admin/appeals | Khi duyệt đơn cứu xét bị lỗi, không chỉ hiện “không thể duyệt đơn cứu xét” mà phải hiện lỗi thực sự phía sau. | `appeals.js` dùng `appealErrorMessage(res, ...)` để hiển thị message và code từ API khi duyệt lỗi. | `appealController.approveAppeal` trả lỗi validation nghiệp vụ trực tiếp, còn lỗi Prisma/RBTV đi qua `sendErrorResponse` với detail `Không thể duyệt...: <error.message>`. | Không cần DB mới; đã rà luồng `DONCUUXETDANGKY`, `PHIEUDANGKY`, `CHITIETDANGKY`. | Đã chạy `node --check`, Pug compile; tình huống lớp đầy/trùng lịch/thiếu phiếu trả message cụ thể. |
| REQ-APPEAL-002 | [x] | /admin/appeals | Thiếu tra cứu đơn cứu xét; thiết kế tra cứu tương tự bên quản lý đăng ký môn học. | `appeals.pug` có filter panel theo MSSV, họ tên, lớp, môn, học kỳ, lý do, loại đơn, trạng thái; `appeals.js` build query và phân trang client. | `appealController.buildAppealWhere` nhận `searchScope`, `search`, `MaHocKy`, `LoaiDon`, `TrangThai`; `viewController.adminAppeals` truyền default học kỳ. | Không cần DB mới; dùng join/include `DONCUUXETDANGKY`, `SINHVIEN`, `PHIEUDANGKY`, `HOCKY`, `LOP`. | Đã chạy `node --check`, Pug compile; tìm/lọc từng tiêu chí, reset filter và phân trang qua `/api/appeals`. |
| REQ-APPEAL-003 | [x] | /admin/appeals | Hiệu chỉnh giao diện thân thiện với người dùng sau các thao tác trên. | Filter, bảng, badge trạng thái, panel thời gian cứu xét và nút duyệt/từ chối dùng layout giống đăng ký. | Response API duyệt/từ chối/list dùng message cụ thể; route `/api/appeals` đã có admin middleware. | Không cần DB mới. | Đã chạy `node --check`, Pug compile; kiểm tra duyệt thành công, duyệt lỗi, từ chối, tra cứu và reload trạng thái. |

## 13. Trang quản lý học phí / công nợ học phí

File đã sửa/kiểm tra trong nhóm: `src/views/pages/admin/tuition.pug`; `src/public/js/admin/tuition.js`; `src/controllers/viewController.js`; `src/controllers/tuitionController.js`; `src/routes/tuitionRoutes.js`; `src/utils/paymentRules.js`; `prisma/schema.prisma`; `src/views/partials/sidebar-admin.pug`.

| Mã | Trạng thái | Trang/module liên quan | Nội dung yêu cầu gốc | Frontend cần kiểm tra/sửa | Backend/API cần kiểm tra/sửa | Database/migration/seed liên quan | Cách kiểm thử |
|---|---|---|---|---|---|---|---|
| REQ-TUITION-001 | [x] | /admin/tuition | Đổi tên trang quản lý học phí thành Công nợ học phí. | `tuition.pug` tiêu đề bảng là `Công nợ học phí`; sidebar label là `Công nợ học phí`. | `viewController.adminTuition` render title `Công nợ học phí`, giữ route `/admin/tuition` để không gãy link cũ. | Không cần DB mới. | Đã compile Pug; mở sidebar/trang thấy tên Công nợ học phí. |
| REQ-TUITION-002 | [x] | /admin/tuition | Thêm select bên trái ô tìm kiếm để tìm kiếm theo MSSV hoặc họ tên. | `tuition.pug` có `#search-field` với `MSSV`, `Họ tên`; `tuition.js` gửi `searchField`. | `viewController.adminTuition` và `tuitionController.getAllTuition` lọc theo `SINHVIEN.MaSv` hoặc `SINHVIEN.HoTen`. | Không cần DB mới; dùng join sinh viên/phiếu đăng ký. | Đã chạy `node --check`, Pug compile; tìm MSSV/họ tên giữ phân trang query. |
| REQ-TUITION-003 | [x] | /admin/tuition | Filter tất cả học kỳ mặc định là học kỳ hiện tại khi mới vào. | Dropdown học kỳ nhận `MaHocKy` default từ view khi không có query; người dùng vẫn chọn tất cả bằng option rỗng. | `viewController.adminTuition` dùng `getDefaultSemesterCode(semesters)` khi request chưa có `MaHocKy`. | Không cần DB mới; dùng dữ liệu `HOCKY` hiện có. | Đã rà code default học kỳ; vào trang không query chọn kỳ hiện tại/gần nhất, đổi filter vẫn hoạt động. |
| REQ-TUITION-004 | [x] | /admin/tuition | Khi nhấn nút xem trong thao tác, giao diện chi tiết học phí phải cân xứng, thân thiện hơn. | `tuition.js` render modal có 3 thẻ tổng quan, info-list sinh viên/học kỳ/hạn/trạng thái, bảng môn và bảng phiếu thu. | `tuitionController.getTuitionById`/`buildTuitionDetail` trả công nợ, phiếu thu, dòng đăng ký, miễn giảm; đã sửa trạng thái API `Chưa đóng`/`Đóng một phần` cho khớp filter. | Không cần DB mới; tính từ `PHIEUDANGKY`, `CHITIETDANGKY`, `PHIEUTHUHOCPHI`. | Đã chạy `node --check`, Pug compile; bấm xem nhiều sinh viên/học kỳ, số tiền khớp bảng và phiếu thu. |
| REQ-TUITION-005 | [x] | /admin/tuition | Hiệu chỉnh giao diện thân thiện với người dùng sau các thao tác trên. | Toolbar search/filter, bảng badge trạng thái nợ và modal detail dùng layout admin chung; số tiền định dạng `vi-VN`. | Response list/detail công nợ có message lỗi chuẩn qua `sendErrorResponse`, trạng thái công nợ nhất quán admin/API. | Không cần DB mới ngoài các mục trên. | Đã chạy `node --check`, Pug compile; kiểm tra search/filter/detail desktop/mobile. |

## 13.1. Trang quản lý phiếu thu / thanh toán

File đã sửa/kiểm tra trong nhóm: `src/views/pages/admin/payments.pug`; `src/public/js/admin/payments.js`; `src/controllers/paymentController.js`; `src/routes/paymentRoutes.js`; `src/views/partials/sidebar-admin.pug`; `src/views/pages/student/my-payments.pug`; `src/public/js/student/my-payments.js`; `src/views/pages/student/my-tuition.pug`; `src/public/js/student/my-tuition.js`; `src/controllers/tuitionController.js`; `src/utils/paymentRules.js`; `prisma/schema.prisma`.

| Mã | Trạng thái | Trang/module liên quan | Nội dung yêu cầu gốc | Frontend cần kiểm tra/sửa | Backend/API cần kiểm tra/sửa | Database/migration/seed liên quan | Cách kiểm thử |
|---|---|---|---|---|---|---|---|
| REQ-PAYMENT-001 | [x] | /admin/payments, /admin/tuition | Đổi thứ tự quản lý phiếu thu lên trên quản lý công nợ học phí trong sidebar. | `sidebar-admin.pug` đã có Phiếu thu trước Công nợ học phí, active state giữ theo route. | Không cần API mới. | Không cần DB mới. | Đã rà sidebar; click `/admin/payments` và `/admin/tuition` dùng route hiện có. |
| REQ-PAYMENT-002 | [x] | /admin/payments | Khi nhấn tạo hàng loạt phiếu thu, phải tạo phiếu thu hàng loạt cho sinh viên để sinh viên có thể vào thanh toán. | `payments.pug`/`payments.js` có modal bulk chọn học kỳ, không auto reload, hiển thị tổng số, dòng tạo thành công và dòng bỏ qua. | `paymentController.createBulkPayments` tạo `PHIEUTHUHOCPHI` theo công nợ đủ điều kiện, tránh phiếu đang hiệu lực, trả từng dòng gồm MSSV/họ tên/số phiếu/lý do. | Không cần migration; model `PHIEUTHUHOCPHI` đã có FK tới `PHIEUDANGKY`/`SINHVIEN`, trạng thái và trường checkout. | Đã chạy `node --check`; tạo bulk cần kiểm thử dữ liệu thật để thấy created/skipped từng sinh viên. |
| REQ-PAYMENT-003 | [x] | /student/my-payments, /student/my-tuition | Sinh viên chọn phương thức thanh toán và thanh toán từ phiếu thu đã tạo. | `my-tuition.pug`/`my-tuition.js` hiển thị phiếu có thể thanh toán, modal chọn tiền mặt/QR/VNPAY/ZaloPay; `my-payments` hiển thị lịch sử. | `paymentController.checkoutPayment` validate quyền sinh viên, cập nhật provider/channel/giao dịch/checkout URL/QR. | `PHIEUTHUHOCPHI` đã có `PaymentProvider`, `PaymentChannel`, `CheckoutUrl`, `QrPayload`, `MaGiaoDich`; không sửa schema. | Đã chạy `node --check`; kiểm thử student chọn phương thức tạo trạng thái chờ xác nhận hoặc chuyển cổng thanh toán. |
| REQ-PAYMENT-004 | [x] | /admin/payments, /student/my-payments | Thanh toán xong nếu thành công/thất bại thì hiển thị ở danh sách phiếu thu. | Bảng admin và student hiển thị trạng thái, mã giao dịch, thời điểm xác nhận; detail admin vẫn có đủ thông tin phiếu. | Callback VNPAY/ZaloPay, admin confirm/fail/cancel cập nhật `TrangThai`, `NgayXacNhan`, `NgayCapNhat`, `MaGiaoDich`. | Không cần seed/migration; trạng thái dùng constants hiện có. | Đã chạy `node --check`; mô phỏng confirm/fail/callback rồi reload danh sách để thấy trạng thái/giao dịch/thời điểm. |
| REQ-PAYMENT-005 | [x] | /admin/tuition, /admin/payments | Sau thanh toán phải cập nhật công nợ học phí, xem còn nợ hay đã đủ. | `tuition.js`/`tuition.pug` hiển thị tổng phải đóng, đã đóng, còn nợ và phiếu thu trong detail. | `tuitionController` tính công nợ từ tổng phiếu thu thành công trừ hoàn tiền; `paymentController` cập nhật trạng thái thành công/thất bại thật, không chỉ sửa UI. | Không cần DB mới; công thức dựa trên `PHIEUDANGKY.TongTienPhaiDong` và `PHIEUTHUHOCPHI` thành công/hoàn tiền. | Đã chạy `node --check`; thanh toán/confirm xong reload công nợ để thấy còn nợ hoặc đã đóng đủ. |
| REQ-PAYMENT-006 | [x] | /admin/payments | Hiệu chỉnh giao diện thân thiện với người dùng sau các thao tác trên. | Bulk modal có kết quả từng dòng và nút tải lại; bảng có mã giao dịch/thời điểm xác nhận; toolbar/trạng thái giữ layout admin chung. | Response bulk/detail/callback/confirm trả message và dữ liệu rõ; không đổi permission/route ngoài luồng hiện có. | Không cần DB mới ngoài các trường đã có. | Đã chạy `node --check`; Pug compile và Prisma validate sẽ chạy sau cập nhật checklist. |

## 14. Trang quản lý đơn giá tín chỉ

File đã sửa trong nhóm: `src/views/pages/admin/pricing.pug`; `src/public/js/admin/pricing.js`; `src/public/css/admin/pricing.css`.

| Mã | Trạng thái | Trang/module liên quan | Nội dung yêu cầu gốc | Frontend cần kiểm tra/sửa | Backend/API cần kiểm tra/sửa | Database/migration/seed liên quan | Cách kiểm thử |
|---|---|---|---|---|---|---|---|
| REQ-PRICING-001 | [x] | /admin/pricing | Thời gian ở phần sửa thiếu giờ phút giây. | Bảng và modal sửa hiển thị `Sửa lúc` đủ ngày/tháng/năm giờ:phút:giây; modal sửa có khối audit `Sửa bởi/Sửa lúc`. | Không cần API mới; dùng dữ liệu `NgayCapNhat`, `NguoiCapNhat`. | Không cần migration. | `node --check`; Pug compile; mở modal sửa kiểm tra thời gian đầy đủ. |
| REQ-PRICING-002 | [x] | /admin/pricing | Hiệu chỉnh giao diện thân thiện với người dùng. | Bổ sung khối audit đọc rõ, giữ layout filter/table/modal gọn và nhất quán style admin. | Không cần API mới. | Không cần migration. | Pug compile; kiểm tra modal thêm không hiện audit, modal sửa hiện audit. |

## 15. Trang quản lý đối tượng ưu tiên

File đã sửa trong nhóm: `src/controllers/beneficiaryController.js`; `src/controllers/viewController.js`; `src/views/pages/admin/beneficiaries.pug`; `src/public/js/admin/beneficiaries.js`; `src/public/css/theme.css`.

| Mã | Trạng thái | Trang/module liên quan | Nội dung yêu cầu gốc | Frontend cần kiểm tra/sửa | Backend/API cần kiểm tra/sửa | Database/migration/seed liên quan | Cách kiểm thử |
|---|---|---|---|---|---|---|---|
| REQ-BENEFICIARY-001 | [x] | /admin/beneficiaries | Bên trái ô tìm kiếm, thêm select để tìm theo mã hoặc tên đối tượng. | Toolbar có select `Tất cả/Mã/Tên đối tượng` trước ô tìm kiếm và giữ query `searchField` khi lọc/phân trang. | API/list view nhận `searchField=MaDoiTuong/TenDoiTuong/all`. | Không cần migration. | `node --check`; Pug compile; tìm theo từng tiêu chí. |
| REQ-BENEFICIARY-002 | [x] | /admin/beneficiaries, /api/beneficiaries | Khi thêm/sửa bỏ nhập độ ưu tiên; độ ưu tiên tự tính theo tỉ lệ giảm; cập nhật lại toàn bộ bản ghi. | Form bỏ input `DoUuTien`, thay bằng trạng thái tự động. | `createBeneficiary`, `updateBeneficiary`, `deleteBeneficiary` chạy transaction và recompute `DoUuTien` cho toàn bộ bản ghi chưa xóa theo `TiLeGiamHocPhi` giảm dần. | Không cần migration. | `node --check`; tạo/sửa tỉ lệ giảm và kiểm tra bảng sắp xếp/độ ưu tiên cập nhật lại. |
| REQ-BENEFICIARY-003 | [x] | /admin/beneficiaries | Mã đối tượng khi sửa phải tô xám vì không được sửa mã; click vùng tô xám hiện thông báo. | `MaDoiTuong` dùng `readonly`, class readonly và `data-readonly-message`; click field hiện toast qua `AdminUI`. | `updateBeneficiary` chặn request cố đổi `MaDoiTuong`. | Không cần migration. | Mở modal sửa, click mã đối tượng thấy thông báo; gửi API đổi mã bị trả lỗi. |

## 16. Trang báo cáo thống kê

File đã sửa trong nhóm: `src/controllers/viewController.js`; `src/routes/viewRoutes.js`; `src/views/pages/admin/reports.pug`; `src/views/pages/admin/reports-incomplete-tuition.pug`; `src/views/partials/sidebar-admin.pug`; `src/public/js/admin/reports.js`; `src/utils/permissionCatalog.js`.

| Mã | Trạng thái | Trang/module liên quan | Nội dung yêu cầu gốc | Frontend cần kiểm tra/sửa | Backend/API cần kiểm tra/sửa | Database/migration/seed liên quan | Cách kiểm thử |
|---|---|---|---|---|---|---|---|
| REQ-REPORT-001 | [x] | /admin/reports, /admin/reports/incomplete-tuition | Tách phần “Sinh viên chưa hoàn thành học phí” thành một mục riêng trên sidebar thuộc khung báo cáo. | Sidebar khung Báo cáo có mục `Chưa hoàn thành học phí`; trang thống kê chính chỉ còn link điều hướng; trang mới chứa filter, tổng quan, bảng, export Excel/PDF. | Thêm route view `/admin/reports/incomplete-tuition`, render cùng dữ liệu học kỳ/khoa/ngành; quyền `ADMIN_REPORTS` nhận path mới. | Không cần migration. | Pug compile; mở hai route, kiểm tra sidebar active và bảng nợ tải riêng. |
| REQ-REPORT-002 | [x] | /admin/reports, /admin/reports/incomplete-tuition | Hiệu chỉnh giao diện thân thiện với người dùng. | Trang nợ có toolbar/filter/action riêng; `reports.js` có guard để mỗi trang chỉ gọi phần dữ liệu có DOM tương ứng. | Không cần API mới; dùng lại `/api/dashboard/incomplete-tuition`. | Không cần migration. | `node --check`; Pug compile; đổi học kỳ/filter trên từng trang không lỗi DOM thiếu. |

Ghi chú kiểm tra Cụm 9: `node --check`, Pug compile và `npx prisma validate` đã pass. `npm test` vẫn fail do script placeholder `Error: no test specified`; `npm run lint` và `npm run build` chưa có trong `package.json`.

## 27. Trang quản lý thông báo

File đã sửa trong nhóm: `src/config/init.sql`; `src/config/database.js`; `src/utils/notificationEvents.js`; `src/controllers/notificationController.js`; `src/controllers/registrationController.js`; `src/controllers/paymentController.js`; `src/controllers/viewController.js`; `src/views/pages/admin/notifications.pug`; `src/public/js/admin/notifications.js`.

| Mã | Trạng thái | Trang/module liên quan | Nội dung yêu cầu gốc | Frontend cần kiểm tra/sửa | Backend/API cần kiểm tra/sửa | Database/migration/seed liên quan | Cách kiểm thử |
|---|---|---|---|---|---|---|---|
| REQ-NOTIFICATION-001 | [x] | /admin/notifications | Seed thông báo có vấn đề; chỉ nên seed thông báo về hạn thu học phí, hạn đăng ký học phần, hạn tương tự; thông báo khi thanh toán/đăng ký môn phải là thông báo tự động. | Bảng thông báo hiển thị nguồn thủ công/tự động; form thủ công chỉ chọn nhóm hạn hợp lệ, không cho sửa thông báo tự động. | `registrationController.registerCourse` sinh thông báo đăng ký học phần tự động; `paymentController` sinh thông báo thanh toán thành công/thất bại theo callback hoặc xác nhận admin; `notificationController` chặn sửa loại `auto_*`. | `init.sql` bỏ seed thông báo đăng ký/thanh toán thành công, chỉ seed hạn đăng ký, hạn cứu xét, hạn thu học phí, hạn bảo trì; `database.js` cho phép thông báo broadcast không có `MaTaiKhoanNhan`. | Kiểm tra seed `THONGBAO` không còn bản ghi thành công theo sự kiện; đăng ký môn sinh `auto_dang_ky_mon`; thanh toán/xác nhận/thất bại sinh `auto_thanh_toan_hoc_phi`; thông báo tự động không sửa thủ công. |
| REQ-NOTIFICATION-002 | [x] | /admin/notifications | Hiệu chỉnh giao diện thân thiện sau thao tác trên. | Thêm filter nguồn, cột nguồn, cột hết hạn, badge loại/nguồn; vô hiệu nút sửa với thông báo tự động; preview hiển thị nhóm hạn. | `viewController.adminNotifications` hỗ trợ lọc `Nguon=manual/auto`. | Không cần migration ngoài seed/bootstrap trên. | Mở `/admin/notifications`, lọc theo nguồn/loại, tạo thông báo hạn thủ công, xem bảng không nhầm thông báo tự động. |

## 28. Trang tham số hệ thống

File đã sửa trong nhóm: `src/controllers/settingsController.js`; `src/controllers/paymentController.js`; `src/controllers/registrationController.js`; `src/controllers/viewController.js`; `src/views/pages/admin/settings.pug`; `src/public/js/admin/settings.js`; `src/public/css/theme.css`.

| Mã | Trạng thái | Trang/module liên quan | Nội dung yêu cầu gốc | Frontend cần kiểm tra/sửa | Backend/API cần kiểm tra/sửa | Database/migration/seed liên quan | Cách kiểm thử |
|---|---|---|---|---|---|---|---|
| REQ-SETTING-001 | [x] | /admin/settings | Khi thay đổi các trường dữ liệu trong trang tham số hệ thống thì các trường ràng buộc cũng phải thay đổi theo. | Trang settings hiển thị bảng ràng buộc đang dùng từng tham số; validate frontend đồng nhất backend. | `settingsController` trả metadata tác động, validate số nguyên và mã môn Anh văn tồn tại; `registrationController` dùng `SoTinChiDangKyToiDa`, `SoTinChiDangKyToiDaKhiVuot`, `DanhSachMonAnhVanBatBuoc`, `NamKiemTraAnhVan`, `GioiHanTinChiChuaDatAnhVan`; `paymentController` dùng `SoTinChiDangKyToiThieu` ở tạo phiếu thu đơn lẻ/hàng loạt, checkout và xác nhận; `curriculumService` đang dùng `GioiHanTinChiNoKhoaLuan`. | Không thêm tham số hạn đăng ký/hạn đóng học phí vì code xác định các hạn này thuộc `HOCKY`, không thuộc `THAMSO`; không cần migration. | Đổi từng tham số và kiểm tra: trần tín chỉ đăng ký thay đổi ngay, giới hạn cứng khi vượt được chặn, danh sách/mốc Anh văn đổi giới hạn, thanh toán chặn phiếu dưới tín chỉ tối thiểu, điều kiện khóa luận đọc giới hạn nợ mới. |

## 29. Trang quản lý năm học

File đã sửa/kiểm tra trong nhóm: `prisma/schema.prisma`; `src/config/init.sql`; `src/config/database.js`; `src/controllers/viewController.js`; `src/controllers/semesterController.js`; `src/views/pages/admin/academic-years.pug`; `src/public/js/admin/academic-years.js`; `src/public/js/main.js`; `src/public/css/theme.css`.

| Mã | Trạng thái | Trang/module liên quan | Nội dung yêu cầu gốc | Frontend cần kiểm tra/sửa | Backend/API cần kiểm tra/sửa | Database/migration/seed liên quan | Cách kiểm thử |
|---|---|---|---|---|---|---|---|
| REQ-ACADEMICYEAR-001 | [x] | /admin/academic-years | Thêm select cạnh trái ô tìm kiếm để tìm kiếm theo mã hoặc theo tên năm học. | `academic-years.pug` có `#year-search-field`; `academic-years.js` gửi `searchField` khi lọc. | `viewController.adminAcademicYears` và `semesterController.getAcademicYears` lọc theo `MaNamHoc` hoặc `TenNamHoc`. | Không cần DB mới. | Chọn từng tiêu chí, nhập từ khóa, kiểm tra URL/API có `searchField`; `node --check`; Pug compile. |
| REQ-ACADEMICYEAR-002 | [x] | /admin/academic-years | Xóa cột học kỳ. | Bảng `academic-years.pug` không còn cột học kỳ/`SoHocKy`; colspan đã cập nhật. | `viewController.adminAcademicYears` không còn include `_count.HOCKY` cho bảng này. | Không cần DB mới. | Compile Pug và rà bảng chỉ còn các cột năm học/trạng thái/audit/thao tác. |
| REQ-ACADEMICYEAR-003 | [x] | /admin/academic-years | Khi sửa năm học, các trường không được phép sửa phải bôi xám và hiện thông báo khi click. | `#year-ma` có `data-readonly-message`; `academic-years.js` set `readOnly` và dùng `AdminUI.markReadonlyFields`; `main.js` khởi tạo `AdminUI.initReadonlyNotices`. | `semesterController.updateAcademicYear` cập nhật theo `req.params.id`, không nhận đổi `MaNamHoc`. | Không cần DB mới. | Mở sửa, mã năm học tô xám; click mã hiện toast; gửi PUT kèm mã khác không đổi khóa chính. |
| REQ-ACADEMICYEAR-004 | [x] | /admin/academic-years | Click vào dòng bản ghi thì popup/modal chi tiết chỉ xem. | `academic-years.pug` gắn `data-record`; `academic-years.js` dùng `AdminUI.attachRowDetailHandlers`. | Dữ liệu list có đủ trường để dựng chi tiết. | Không cần DB mới. | Click dòng ngoài nút thao tác, modal chi tiết mở đúng; nút sửa/xóa không kích hoạt detail. |
| REQ-ACADEMICYEAR-005 | [x] | /admin/academic-years | Bổ sung cột sửa bởi ai, sửa vào thời điểm đầy đủ giờ phút giây, ngày tháng năm. | Bảng và detail hiển thị `NguoiCapNhatTen`/`NguoiCapNhat`, `NgayCapNhat` với giây. | `semesterController` ghi `updateAudit(req)` khi tạo/sửa năm học; `viewController`/API attach tên người cập nhật. | `NAMHOC` có `NguoiCapNhat`, `NgayCapNhat` trong Prisma schema, `init.sql`, bootstrap `database.js`. | `npx prisma validate`; sửa năm học rồi kiểm tra cột người sửa/thời điểm sửa định dạng đủ giây. |
| REQ-ACADEMICYEAR-006 | [x] | /admin/academic-years | Hiệu chỉnh giao diện thân thiện với người dùng sau các thao tác trên. | Toolbar search/filter, bảng audit, modal form và detail modal dùng style admin chung. | Không thêm API ngoài các endpoint năm học hiện có. | Không cần DB mới ngoài audit ở REQ-ACADEMICYEAR-005. | Pug compile, JS syntax, rà tương tác search/filter/modal. |

## 30. Trang quản lý khoa

File đã sửa/kiểm tra trong nhóm: `src/controllers/viewController.js`; `src/controllers/facultyController.js`; `src/views/pages/admin/faculties.pug`; `src/public/js/admin/faculties.js`; `src/public/js/main.js`; `src/public/css/theme.css`.

| Mã | Trạng thái | Trang/module liên quan | Nội dung yêu cầu gốc | Frontend cần kiểm tra/sửa | Backend/API cần kiểm tra/sửa | Database/migration/seed liên quan | Cách kiểm thử |
|---|---|---|---|---|---|---|---|
| REQ-FACULTY-001 | [x] | /admin/faculties | Thêm select bên trái ô tìm kiếm để tìm kiếm theo mã khoa, tên khoa, hay tên viết tắt. | `faculties.pug` có `#faculty-search-field`; `faculties.js` gửi `searchField`. | `viewController.adminFaculties` và `facultyController.getAllFaculties` lọc theo `MaKhoa`, `TenKhoa`, `TenVietTat`. | Không cần DB mới. | Chọn từng tiêu chí search; `node --check`; Pug compile. |
| REQ-FACULTY-002 | [x] | /admin/faculties | Khi sửa khoa, trường không được phép sửa phải bôi xám và hiện thông báo khi click. | `#fac-ma` có `data-readonly-message`; JS set `readOnly` khi edit và dùng `AdminUI.markReadonlyFields`; readonly notice global trong `main.js`. | `facultyController.updateFaculty` cập nhật theo `req.params.id`, không nhận đổi `MaKhoa`. | Không cần DB mới. | Mở sửa khoa, click mã khoa có toast; PUT không thể đổi khóa chính. |
| REQ-FACULTY-003 | [x] | /admin/faculties | Click vào dòng bản ghi thì popup/modal chi tiết chỉ xem. | `faculties.pug` gắn `data-record`; `faculties.js` dùng `AdminUI.attachRowDetailHandlers`. | List trả đủ dữ liệu và `_count` để hiển thị detail. | Không cần DB mới. | Click dòng mở detail; click sửa/xóa không mở detail. |
| REQ-FACULTY-004 | [x] | /admin/faculties | Hiệu chỉnh giao diện thân thiện với người dùng sau các thao tác trên. | Toolbar search theo tiêu chí, bảng audit, modal form/detail dùng style admin chung. | Không thêm API ngoài API khoa hiện có. | Không cần DB mới. | Pug compile, JS syntax, rà thao tác thêm/sửa/search/detail. |

## 31. Trang quản lý ngành học

File đã sửa/kiểm tra trong nhóm: `src/controllers/viewController.js`; `src/controllers/majorController.js`; `src/views/pages/admin/majors.pug`; `src/public/js/admin/majors.js`; `src/public/js/main.js`; `src/public/css/theme.css`.

| Mã | Trạng thái | Trang/module liên quan | Nội dung yêu cầu gốc | Frontend cần kiểm tra/sửa | Backend/API cần kiểm tra/sửa | Database/migration/seed liên quan | Cách kiểm thử |
|---|---|---|---|---|---|---|---|
| REQ-MAJOR-001 | [x] | /admin/majors | Thêm select bên trái ô tìm kiếm để tìm kiếm theo mã ngành, tên ngành, hay tên khoa. | `majors.pug` có `#major-search-field`; `majors.js` gửi `searchField`. | `viewController.adminMajors` và `majorController.getAllMajors` lọc theo `MaNganh`, `TenNganh`, relation `KHOA.TenKhoa`. | Không cần DB mới. | Chọn từng tiêu chí search; kiểm tra query theo tên khoa; `node --check`; Pug compile. |
| REQ-MAJOR-002 | [x] | /admin/majors | Khi sửa ngành, trường không được phép sửa phải bôi xám và hiện thông báo khi click. | `#maj-ma` có `data-readonly-message`; JS set `readOnly` khi edit và dùng readonly style/toast chung. | `majorController.updateMajor` cập nhật theo `req.params.id`, không nhận đổi `MaNganh`. | Không cần DB mới. | Mở sửa ngành, click mã ngành có toast; PUT không thể đổi khóa chính. |
| REQ-MAJOR-003 | [x] | /admin/majors | Nút CTDT Chương trình đào tạo khi nhấn vào chưa hiện đúng chương trình đào tạo. | Nút CTDT truyền `data-major-id`/`data-major-name`; `selectCurriculumMajor` chọn đúng ngành, cập nhật tiêu đề và gọi `loadCurriculum()`. | `majorController.getCurriculum` nhận `MaNganh` và trả chương trình theo ngành; route `/api/majors/curriculum/items`. | Không cần DB mới. | Bấm CTDT ở từng ngành, kiểm tra `#curriculum-major` đổi đúng và bảng CTDT tải đúng môn của ngành. |
| REQ-MAJOR-004 | [x] | /admin/majors | Click vào dòng bản ghi thì popup/modal chi tiết chỉ xem. | `majors.pug` gắn `data-record`; `majors.js` dùng `AdminUI.attachRowDetailHandlers`. | List include `KHOA` và `_count.SINHVIEN` để hiển thị detail. | Không cần DB mới. | Click dòng mở detail; click CTDT/sửa/xóa không mở detail. |
| REQ-MAJOR-005 | [x] | /admin/majors | Hiệu chỉnh giao diện thân thiện với người dùng sau các thao tác trên. | Toolbar search/filter khoa, bảng audit, khối CTDT, modal form/detail dùng style admin chung. | Không thêm API ngoài API ngành/CTDT hiện có. | Không cần DB mới. | Pug compile, JS syntax, rà search/filter/modal/CTDT. |

## 32. Trang quản lý tiết học

File đã sửa/kiểm tra trong nhóm: `src/controllers/viewController.js`; `src/controllers/periodController.js`; `src/views/pages/admin/periods.pug`; `src/public/js/admin/periods.js`; `src/public/js/main.js`; `src/public/css/theme.css`.

| Mã | Trạng thái | Trang/module liên quan | Nội dung yêu cầu gốc | Frontend cần kiểm tra/sửa | Backend/API cần kiểm tra/sửa | Database/migration/seed liên quan | Cách kiểm thử |
|---|---|---|---|---|---|---|---|
| REQ-PERIOD-001 | [x] | /admin/periods | Có select bên trái ô tìm kiếm để tìm kiếm theo mã tiết hoặc tên tiết. | `periods.pug` có `#period-search-field`; `periods.js` gửi `searchField`. | `viewController.adminPeriods` và `periodController.getPeriods` lọc theo `MaTiet`, `TenTiet`. | Không cần DB mới. | Chọn mã tiết/tên tiết, nhập từ khóa, kiểm tra query; `node --check`; Pug compile. |
| REQ-PERIOD-002 | [x] | /admin/periods | Khi sửa tiết học, các trường không được phép sửa phải bôi xám và hiện thông báo khi click. | `#period-ma` có `data-readonly-message`; JS set `readOnly` khi edit và dùng readonly style/toast chung. | `periodController.updatePeriod` xóa `data.MaTiet`, cập nhật theo `req.params.id`. | Không cần DB mới. | Mở sửa tiết học, click mã tiết có toast; PUT không thể đổi `MaTiet`. |
| REQ-PERIOD-003 | [x] | /admin/periods | Click vào dòng bản ghi thì popup/modal chi tiết chỉ xem. | `periods.pug` gắn `data-record`; `periods.js` dùng `AdminUI.attachRowDetailHandlers`. | List trả giờ bắt đầu/kết thúc dạng text để hiển thị detail. | Không cần DB mới. | Click dòng mở detail; click sửa/xóa không mở detail. |
| REQ-PERIOD-004 | [x] | /admin/periods | Hiệu chỉnh giao diện thân thiện với người dùng sau các thao tác trên. | Toolbar search, bảng, modal form/detail dùng style admin chung. | Không thêm API ngoài API tiết học hiện có. | Không cần DB mới. | Pug compile, JS syntax, rà thao tác search/detail/sửa. |

## 33. Ghi chú yêu cầu bị lặp tên "Trang Quản lý tiết học"

Giả định: block yêu cầu "Thiếu filter theo trạng thái chốt đăng ký, theo trạng thái mở thu" không thuộc `TIETHOC` mà thuộc học kỳ/đợt đăng ký. Code xử lý đúng ở `/admin/semesters` vì dữ liệu nằm trên `HOCKY.NgayChotDangKy`, `HOCKY.MoThuHocPhi`, `HOCKY.NgayMoThuHocPhi` và hạn học phí.

File đã sửa/kiểm tra trong nhóm: `src/views/pages/admin/semesters.pug`; `src/public/js/admin/semesters.js`; `src/controllers/semesterController.js`.

| Mã | Trạng thái | Trang/module liên quan | Nội dung yêu cầu gốc | Frontend cần kiểm tra/sửa | Backend/API cần kiểm tra/sửa | Database/migration/seed liên quan | Cách kiểm thử |
|---|---|---|---|---|---|---|---|
| REQ-SEMESTER-REGSTATUS-001 | [x] | /admin/semesters | Thiếu filter theo trạng thái chốt đăng ký, theo trạng thái mở thu. | `semesters.pug` thêm select `#semester-registration-finalized`, `#semester-tuition-open`; `semesters.js` gửi query. | `semesterController.buildSemesterWhere` lọc `NgayChotDangKy` null/not null và `MoThuHocPhi` true/false. | Không cần DB mới vì cột đã tồn tại trên `HOCKY`. | Chọn từng filter, kiểm tra API `/api/semesters` trả đúng nhóm đã chốt/chưa chốt và mở thu/chưa mở thu; `node --check`; Pug compile. |
| REQ-SEMESTER-REGSTATUS-002 | [x] | /admin/semesters | Click vào dòng bản ghi thì popup/modal chi tiết có bản ghi đó. | `semesters.js` gắn `AdminUI.attachRowDetailHandlers` cho `.semester-list-row[data-record-index]`, detail chỉ xem. | API list học kỳ đã trả đủ dữ liệu detail. | Không cần DB mới. | Click dòng học kỳ ngoài nút thao tác, modal chi tiết mở đúng; click sửa/xóa không mở detail. |
| REQ-SEMESTER-REGSTATUS-003 | [x] | /admin/semesters | Hiệu chỉnh giao diện thân thiện với người dùng sau các thao tác trên. | Filter mới nằm trong filter panel hiện có, dùng badge/detail modal style admin chung. | Không thêm endpoint mới. | Không cần DB mới. | Pug compile, JS syntax, rà filter/reset/detail. |

## 34. Cum 7 - Hoc vu con thieu

File da sua trong nhom: `prisma/schema.prisma`; `src/config/init.sql`; `src/config/database.js`; `src/controllers/viewController.js`; `src/controllers/classController.js`; `src/controllers/majorController.js`; `src/controllers/completedCourseController.js`; `src/controllers/courseController.js`; `src/services/curriculumService.js`; `src/views/pages/admin/classes.pug`; `src/views/pages/admin/curriculum-programs.pug`; `src/views/pages/admin/completed-courses.pug`; `src/public/js/admin/classes.js`; `src/public/js/admin/curriculum-programs.js`; `src/public/js/admin/completed-courses.js`; `src/public/css/admin/classes.css`; `src/public/css/admin/curriculum-programs.css`; `src/public/css/admin/course-tools.css`.

| Ma | Trang thai | Trang/module lien quan | Noi dung yeu cau goc | Frontend can kiem tra/sua | Backend/API can kiem tra/sua | Database/migration/seed lien quan | Cach kiem thu |
|---|---|---|---|---|---|---|---|
| REQ-CLASS-001 | [x] | /admin/classes | Bo sung filter/sap xep danh sach lop hoc theo si so tang dan va giam dan. | `classes.pug` co select `#filter-capacity-sort`; `classes.js` gui `capacitySort`. | `viewController.adminClasses` tinh `SoLuongDaDangKy`, sap xep `capacity_asc/capacity_desc` va phan trang theo ket qua sap xep. | Khong can DB moi. | Chon tang/giam dan, doi trang va doi hoc ky de kiem tra thu tu si so dung. |
| REQ-CLASS-002 | [x] | /admin/classes | Click vao dong ban ghi thi mo popup/modal chi tiet chi xem. | Dong bang co `.clickable-row`; modal `#class-detail-modal`; nut thao tac chan bubbling. | `classController.getClassById` tra mon hoc, khoa, hoc ky, lich hoc va so dang ky de dung detail. | Khong can DB moi. | Click dong mo detail dung lop; click mo/dong/sua khong mo detail kem. |
| REQ-CLASS-003 | [x] | /admin/classes | Hieu chinh UI than thien voi nguoi dung. | Bo cuc filter si so, hover dong, modal detail va lich hoc dung style chung. | Khong them endpoint ngoai API lop hien co. | Khong can DB moi. | Mo trang lop hoc tren desktop/mobile, kiem tra filter/action/modal khong chong cheo. |
| REQ-CURRICULUM-001 | [x] | /admin/curriculum-programs | Them select ben trai o tim kiem theo ma nganh, ten nganh, ma mon, ten mon. | `#cp-search-field` nam canh o tim kiem; JS gui `searchField`. | `viewController.adminCurriculumPrograms` loc theo field duoc chon. | Khong can DB moi. | Tim tung tieu chi va kiem tra URL/query cung ket qua bang. |
| REQ-CURRICULUM-002 | [x] | /admin/curriculum-programs | Khi sua chuong trinh hoc, truong khong duoc sua phai to xam va bao loi khi click. | `cp-item-major` va picker mon hoc vao trang thai locked khi edit; click hien toast. | `majorController.updateCurriculumItem` chan doi `MaNganh`/`MaMonHoc`. | Khong can DB moi. | Mo sua dong CTDT, click truong locked va gui payload doi khoa chinh de kiem tra bi chan. |
| REQ-CURRICULUM-003 | [x] | /admin/curriculum-programs | Bo trang thai bat buoc cua chuong trinh hoc trong database va giao dien. | Bang/form CTDT khong con cot/truong BatBuoc. | API create/update CTDT khong nhan/cap nhat BatBuoc; service khong phu thuoc BatBuoc. | Prisma/init.sql bo cot; bootstrap `database.js` drop column neu ton tai. | `npx prisma validate`; mo CTDT khong thay BatBuoc; DB cu duoc drop cot khi bootstrap. |
| REQ-CURRICULUM-004 | [x] | /admin/curriculum-programs | Khi them mon, select chon mon doi thanh popup tim kiem mon hoc. | `#curriculum-course-picker-modal` cho tim/chon mon; form luu ma mon qua hidden input. | Dung API `/api/courses?all=true&search=...` de nap danh sach mon. | Khong can DB moi. | Bam Them mon, tim ma/ten mon, chon mon va luu. |
| REQ-CURRICULUM-005 | [x] | /admin/curriculum-programs | Hieu chinh UI than thien voi nguoi dung. | Filter/search, form locked va picker dung style chung, khong con select dai. | Khong them endpoint ngoai API hien co. | Khong can DB moi ngoai bo BatBuoc. | Kiem tra luong search/filter/add/edit/delete CTDT. |
| REQ-COMPLETED-001 | [x] | /admin/completed-courses | Them select ben trai o tim kiem theo MSSV, ho ten, hoc ky. | `#search-field` co MSSV/Ho ten/Hoc ky; JS gui `searchField`. | `viewController.adminCompletedCourses` va API list loc theo field tuong ung. | Khong can DB moi. | Tim tung tieu chi, kiem tra query va ket qua. |
| REQ-COMPLETED-002 | [x] | /admin/completed-courses | Xoa filter theo tin chi, khoa, loai LT/TH. | Pug/JS khong con cac filter `filter-tinchi`, `filter-khoa`, `filter-loai`. | View khong doc cac query filter da bo. | Khong can DB moi. | Mo trang va kiem tra toolbar chi con search, hoc ky, ket qua. |
| REQ-COMPLETED-003 | [x] | /admin/completed-courses | Sua filter hoc ky de option hien du ten hoc ky, ma nam hoc va thong tin can thiet, khong trung. | Filter va form hoc ky hien `TenHocKy - MaHocKy - MaNamHoc - TenNamHoc`. | `viewController.adminCompletedCourses` include `NAMHOC` cho semesters. | Khong can DB moi. | Mo dropdown hoc ky, kiem tra nhan khong trung nhau. |
| REQ-COMPLETED-004 | [x] | /admin/completed-courses | Nhap Excel phai co huong dan format va hien tung dong thanh cong/that bai, loi cu the. | Modal import co ghi chu format; preview/render import dung class success/failed va cot trang thai. | `/api/completed-courses/import` doc CSV/TSV/TXT/XLS/XLSX, validate tung dong va tra `errors` kem index/message. | Khong can DB moi. | Import file dung/sai du lieu, kiem tra tung dong Hop le/loi cu the truoc va sau import. |
| REQ-COMPLETED-005 | [x] | /admin/completed-courses | Bo nhap theo lop. | Button/modal/JS nhap theo lop da bo khoi trang mon da hoc. | Khong con luong UI goi roster lop tu trang nay. | Khong can DB moi. | Tim tren UI khong con nut Nhap theo lop; import chi qua file. |
| REQ-COMPLETED-006 | [x] | /admin/completed-courses | Khi sua mon da hoc, truong khong cho sua phai to xam va bao loi khi click. | MSSV, ma mon, hoc ky, lan hoc co `.lockable-field` va toast khi edit. | `completedCourseController.updateCompletedCourse` chan doi `MaSv`, `MaMonHoc`, `MaHocKy`, `LanHoc`. | Khong can DB moi. | Mo sua ban ghi, click truong locked va thu PUT doi khoa de kiem tra bi chan. |
| REQ-COMPLETED-007 | [x] | /admin/completed-courses | Hieu chinh UI than thien voi nguoi dung. | Toolbar gon hon, bang gom theo sinh vien, modal detail va import dung style chung. | Khong them endpoint ngoai import/file/detail hien co. | Khong can DB moi. | Kiem tra search/filter/detail/edit/import tren desktop/mobile. |

## 35. Dot sua UI layout admin ngay 2026-06-04

File da sua/kiem tra trong nhom: `src/public/css/theme.css`; `src/public/css/admin/locations.css`; `src/public/css/admin/open-courses.css`; `src/public/css/admin/semesters.css`; `src/public/css/admin/curriculum-programs.css`; `src/public/css/admin/course-tools.css`; `src/public/css/admin/registrations.css`; cac view admin lien quan; `src/views/partials/sidebar-admin.pug`. Cac route trong danh sach da render HTTP 200 bang tai khoan admin seed.

| Ma | Trang thai | Trang/module lien quan | Noi dung yeu cau cap nhat | Frontend can kiem tra/sua | Backend/API can kiem tra/sua | Database/migration/seed lien quan | Cach kiem thu |
|---|---|---|---|---|---|---|---|
| REQ-UI-POLISH-001 | [x] | /admin/locations/provinces | Bo khoi count du, chi giu mot noi hien tong so tinh/thanh pho. | `locations-provinces.pug`, `locations.css`. | Khong can backend. | Khong can DB moi. | HTML khong con `location-overview`; route 200. |
| REQ-UI-POLISH-002 | [x] | /admin/locations/wards | Bo khoi count du, chi giu mot noi hien tong so phuong/xa; filter khong an chu. | `locations-wards.pug`, `locations.css`. | Khong can backend. | Khong can DB moi. | HTML khong con `location-overview`; toolbar chia search/filter du rong; route 200. |
| REQ-UI-POLISH-003 | [x] | /admin/students | Trang sinh vien chia search/filter toi da 2 dong de select trang thai khong de len nut them. | `students.pug`, `theme.css`. | Khong can backend. | Khong can DB moi. | Route 200; `.admin-toolbar` day filter xuong dong 2 khi co action. |
| REQ-UI-POLISH-004 | [x] | /admin/open-courses | Hoc ky khong bi khuat; bo label thua `Tu khoa`; search/select hien du placeholder. | `open-courses.pug`, `open-courses.css`, `course-tools.css`. | Khong can backend moi. | Khong can DB moi. | Pug compile; route 200; search icon neo dung sau label `Tim theo`. |
| REQ-UI-POLISH-005 | [x] | /admin/prerequisites | Them/dam bao label `Tim theo` cho select va o tim kiem. | `prerequisites.pug`, `course-tools.css`, `theme.css`. | Khong can backend. | Khong can DB moi. | Route 200; DOM co label `Tim theo`. |
| REQ-UI-POLISH-006 | [x] | /admin/rooms | Chia search/filter thanh 2 dong; select tinh trang khong bi nut them phong de len; co `Tim theo`. | `rooms.pug`, `theme.css`, `rooms.js`. | Kiem tra `roomController.js` giu searchField hien co. | Khong can DB moi. | Route 200; select `room-search-field` ton tai. |
| REQ-UI-POLISH-007 | [x] | /admin/lecturers | Chia search/filter thanh 2 dong; co `Tim theo`; select khong an chu. | `lecturers.pug`, `theme.css`, `lecturers.js`. | Kiem tra `lecturerController.js` giu searchField hien co. | Khong can DB moi. | Route 200; select `lecturer-search-field` ton tai. |
| REQ-UI-POLISH-008 | [x] | /admin/periods | Them/dam bao label `Tim theo`. | `periods.pug`, `theme.css`. | Khong can backend. | Khong can DB moi. | Route 200. |
| REQ-UI-POLISH-009 | [x] | /admin/semesters | Hai select `trang thai chot dang ky` va `trang thai mo thu` khong bi co nho. | `semesters.pug`, `semesters.css`. | Khong can backend. | Khong can DB moi. | Route 200; filter grid chia 12 cot/2 dong. |
| REQ-UI-POLISH-010 | [x] | /admin/academic-years | Them/dam bao label `Tim theo`. | `academic-years.pug`, `theme.css`. | Khong can backend. | Khong can DB moi. | Route 200. |
| REQ-UI-POLISH-011 | [x] | /admin/faculties | Them/dam bao label `Tim theo`. | `faculties.pug`, `theme.css`. | Khong can backend. | Khong can DB moi. | Route 200. |
| REQ-UI-POLISH-012 | [x] | /admin/majors | Them/dam bao label `Tim theo`. | `majors.pug`, `theme.css`. | Khong can backend. | Khong can DB moi. | Route 200. |
| REQ-UI-POLISH-013 | [x] | /admin/curriculum-programs | Chia lai do ngang select theo do dai text. | `curriculum-programs.pug`, `curriculum-programs.css`, `theme.css`. | Khong can backend. | Khong can DB moi. | Route 200; filter grid/search field co minmax phu hop. |
| REQ-UI-POLISH-014 | [x] | /admin/completed-courses | Search/select khong qua lon; dan search/select trong toi da 2 dong. | `completed-courses.pug`, `theme.css`. | Khong can backend. | Khong can DB moi. | Route 200. |
| REQ-UI-POLISH-015 | [x] | /admin/appeals | Hai nut tren trang cuu xet dang ky nam sat le trai. | `appeals.pug`, `registrations.css`. | Khong can backend. | Khong can DB moi. | Route 200; `registration-filter-actions` justify left trong appeal filter. |
| REQ-UI-POLISH-016 | [x] | /admin/payments | Select hien du chu; search nho lai; them select tim theo ma phieu thu/MSSV/ho ten. | `payments.pug`, `payments.js`, `theme.css`. | Kiem tra `paymentController.js`, `viewController.js` da nhan `searchField`. | Khong can DB moi. | DOM co `payment-search-field`; route 200. |
| REQ-UI-POLISH-017 | [x] | /admin/tuition | Cong no hoc phi co `Tim theo`; search khong qua lon lam select bi khuat. | `tuition.pug`, `theme.css`. | Khong can backend moi. | Khong can DB moi. | Route 200. |
| REQ-UI-POLISH-018 | [x] | /admin/beneficiaries | Them/dam bao label `Tim theo`. | `beneficiaries.pug`, `theme.css`. | Khong can backend. | Khong can DB moi. | Route 200. |
| REQ-UI-POLISH-019 | [x] | /admin/reports | Bo thong ke dang ky khoi trang bao cao thong ke vi da co trang rieng. | `reports.pug`, `reports.js`, `sidebar-admin.pug`. | Khong can API moi. | Khong can DB moi. | HTML `/admin/reports` khong con `registration-statistics`, `registration-total-receipts`, link `Thong ke dang ky`. |
| REQ-UI-POLISH-020 | [x] | /admin/reports/incomplete-tuition | Search nho lai sang trai; select ben phai hien du text. | `reports-incomplete-tuition.pug`, `theme.css`, `reports.js`. | Khong can backend moi. | Khong can DB moi. | Route 200; `debt-filter-toolbar` co grid rieng. |
| REQ-UI-POLISH-021 | [x] | /admin/users | Them `Tim theo`; select thao tac trong table thu gon. | `users.pug`, `theme.css`. | Khong can backend. | Khong can DB moi. | Route 200; `account-group-control` max-width 220px. |
| REQ-UI-POLISH-022 | [x] | /admin/permissions | Tab nhom chi hien action nhom; tab quyen chi hien action quyen; bo tieu de lon `Quyen truy cap` du. | `permissions.pug`, `theme.css`. | Khong can backend. | Khong can DB moi. | Route 200; action toolbar nam trong tung tab; khong con sidebar/action du tren UI. |

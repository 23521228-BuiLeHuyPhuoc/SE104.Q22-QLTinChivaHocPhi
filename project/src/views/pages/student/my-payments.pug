extends ../../layouts/student

block content
  .page-shell
    .page-heading.compact
      .page-kicker Tài chính
      h2 Phiếu thu của tôi

    .toolbar
      .filter-group
        select#payment-semester-filter(onchange="loadMyPayments(1)")
          option(value="") Tất cả học kỳ

    .card
      .card-header
        h3 Danh sách phiếu thu
      .card-body
        .loading-overlay#loading
          .spinner
        .table-container
          table.data-table#payments-table.hidden
            thead
              tr
                th Số phiếu
                th Học kỳ
                th Số tiền
                th Phương thức
                th Ngày lập
                th Mã giao dịch
                th Xác nhận
                th Trạng thái
                th Ghi chú
                th Thao tác
            tbody#payment-list
        nav.pagination#payments-pagination(style="display:none")

block modals
  #payment-detail-modal.modal-overlay(onclick="if(event.target===this)closePaymentDetailModal()")
    .modal.modal-wide
      .modal-header
        h3#payment-detail-title Chi tiết phiếu thu
        button.modal-close(type="button" onclick="closePaymentDetailModal()" aria-label="Đóng") ×
      .modal-body#payment-detail-body
        .empty-state Đang tải dữ liệu...
      .modal-footer
        button.btn.btn-outline(type="button" onclick="closePaymentDetailModal()") Đóng

block scripts
  script(src="/js/student/my-payments.js")
